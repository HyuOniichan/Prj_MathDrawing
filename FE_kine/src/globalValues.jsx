//options width and height
export const OPTIONS_WIDTH = 200
export const OPTIONS_HEIGHT = window.innerHeight

//canvas width and height 
export const CANVAS_WIDTH = window.innerWidth - OPTIONS_WIDTH
export const CANVAS_HEIGHT = window.innerHeight

//drawStyle ref
export const _drawStyleRef = {
    strokeStyle: 'black',
    lineWidth: 5,
    lineCap: 'round'
}

//ability to do something on canvas
export const _chosen = {
    option: ''
}